apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.nameOverride }}-web
  namespace: {{ .Values.namespace.name }}
spec:
  ports:
    - port: 80
      targetPort: {{ .Values.app.port }}
  selector:
    app.kubernetes.io/name: {{ .Values.nameOverride }}-web
